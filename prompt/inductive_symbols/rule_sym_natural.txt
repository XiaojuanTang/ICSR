r4	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and A has a property $r2$, then A has a relationship $r4$ with C.
r5	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and A has a property $r1$, then A has a relationship $r5$ with C.
r6	If A has a relationship $r3$ with B and A has a property $r2$, then A has a relationship $r6$ with B.
r7	If A has a relationship $r3$ with B and A has a property $r1$, then A has a relationship $r7$ with B.
r8	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and A has a property $r2$, then A has a relationship $r8$ with C.
r9	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and A has a property $r1$, then A has a relationship $r9$ with C.
r10	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and A has a property $r2$, then A has a relationship $r10$ with D.
r11	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and A has a property $r1$, then A has a relationship $r11$ with D.
r12	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and A has a property $r2$, then A has a relationship $r12$ with D.
r13	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and A has a property $r1$, then A has a relationship $r13$ with D.
r14	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and D has a relationship $r3$ with E and A has a property $r2$, then A has a relationship $r14$ with E.
r15	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and D has a relationship $r3$ with E and A has a property $r1$, then A has a relationship $r15$ with E.
r16	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and D has a relationship $r3$ with E and E has a relationship $r3$ with F and A has a property $r2$, then A has a relationship $r16$ with F.
r17	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and D has a relationship $r3$ with E and E has a relationship $r3$ with F and A has a property $r1$, then A has a relationship $r17$ with F.
r18	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and D has a relationship $r3$ with E and A has a property $r2$, then A has a relationship $r18$ with E.
r19	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and D has a relationship $r3$ with E and A has a property $r1$, then A has a relationship $r19$ with E.
r20	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and D has a relationship $r3$ with E and E has a relationship $r3$ with F and F has a relationship $r3$ with G and A has a property $r2$, then A has a relationship $r20$ with G.
r21	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and D has a relationship $r3$ with E and E has a relationship $r3$ with F and F has a relationship $r3$ with G and A has a property $r1$, then A has a relationship $r21$ with G.
r22	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and D has a relationship $r3$ with E and E has a relationship $r3$ with F and A has a property $r2$, then A has a relationship $r22$ with F.
r23	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and D has a relationship $r3$ with E and E has a relationship $r3$ with F and A has a property $r1$, then A has a relationship $r23$ with F.
r24	If A has a relationship $r3$ with B and A has a property $r2$, then A has a relationship $r24$ with B.
r25	If A has a relationship $r3$ with B and A has a property $r1$, then A has a relationship $r25$ with B.
r26	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and A has a property $r2$, then A has a relationship $r26$ with C.
r27	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and A has a property $r1$, then A has a relationship $r27$ with C.
r28	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and A has a property $r2$, then A has a relationship $r28$ with D.
r29	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and A has a property $r1$, then A has a relationship $r29$ with D.
r30	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and A has a property $r2$, then A has a relationship $r30$ with D.
r31	If A has a relationship $r3$ with B and B has a relationship $r3$ with C and C has a relationship $r3$ with D and A has a property $r1$, then A has a relationship $r31$ with D.
